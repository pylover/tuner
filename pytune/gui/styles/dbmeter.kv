   
<DecibelMeter>:
    canvas:
        Color:
            rgb: .2, .2, .5
        # Line:
            # rectangle: self.x, self.y , self.width, self.height
            # width: 2
        Rectangle:
            pos: self.pos
            size: self.width, self.value * self.height / self.max
    Label:
        pos: root.x + root.width + 20, root.y
        text: str(root.value)
               