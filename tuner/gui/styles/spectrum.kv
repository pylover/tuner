#:import Spectrum tuner.gui.spectrum.Spectrum

<Spectrum>:
    lw: self.width / 8.0 / self.lines 
    canvas:
        Color:
            rgba: self.color      
        # Line:
            # rectangle: self.x, self.y , self.width, self.height
            # width: 1
        Line:
            points: self.getpoints(self.buffer,0)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,1)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,2)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,3)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,4)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,5)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,6)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,7)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,8)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,9)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,10)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,11)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,12)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,13)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,14)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,15)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,16)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,17)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,18)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,19)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,20)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,21)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,22)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,23)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,24)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,25)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,26)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,27)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,28)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,29)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,30)
            width: self.lw
        Line:
            points: self.getpoints(self.buffer,31)
            width: self.lw            

    